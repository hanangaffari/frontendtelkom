export const loginUser = (credential,history,setFieldError,setSubmitting) => {
//make checks
const user = {
    name:"hanan gaffari",
    "username":"hanan"
}
const status = true;
if(status === true){

}else{
    
}
}

export const regUser = (credential,history,setFieldError,setSubmitting) => {

}

export const logoutUser = () => {

}